import styled from 'styled-components'

const HeaderTitle = styled.h2`
  font: 400 28px 'PT Sans';

  &:hover {
    cursor: pointer;
  }
`

const styles = {
  root: {
    flexGrow: 1,
  },
};

export { styles, HeaderTitle }
